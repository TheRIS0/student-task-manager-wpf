<Window x:Class="StudentTaskManager.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Task"
        Height="380"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Title" Margin="0,0,0,4" />
        <TextBox Grid.Row="1"
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12"
                 Padding="4,4" />

        <TextBlock Grid.Row="2" Text="Description" Margin="0,0,0,4" />
        <TextBox Grid.Row="3"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12"
                 Padding="4,4"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 MinHeight="60"
                 VerticalScrollBarVisibility="Auto" />

        <TextBlock Grid.Row="4" Text="Due date (optional)" Margin="0,0,0,4" />
        <DatePicker Grid.Row="5"
                    SelectedDate="{Binding DueDate}"
                    Margin="0,0,0,12" />

        <CheckBox Grid.Row="6"
                  Content="Completed"
                  IsChecked="{Binding IsCompleted}"
                  Margin="0,0,0,12" />

        <StackPanel Grid.Row="7"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,8">
            <Button Content="OK"
                    IsDefault="True"
                    Click="OkButton_Click"
                    Padding="12,6"
                    MinWidth="70"
                    Margin="0,0,8,0" />
            <Button Content="Cancel"
                    IsCancel="True"
                    Padding="12,6"
                    MinWidth="70" />
        </StackPanel>
    </Grid>
</Window>
